// Enunciado: Usando for e if, imprima apenas os números pares entre 1 e 20.

// Solução comentada
for (let i = 1; i <= 20; i++) {
  if (i % 2 === 0) { // verifica se o resto da divisão por 2 é zero
    console.log(i);
  }
}
