// EXERCÍCIO 1: Imprimir elementos de um array
// Dado o array de nomes, use forEach para imprimir cada nome no console

const nomes = ['Ana', 'Bruno', 'Carlos', 'Diana', 'Eduardo'];

// Solução:
nomes.forEach(function(nome) {
  console.log(nome);
});

// Ou usando arrow function:
// nomes.forEach(nome => console.log(nome));

